# Environment Configuration for VyManager
# This file should be customized for your deployment

# ============================================================================
# Application Environment
# ============================================================================
NODE_ENV=production
VYMANAGER_ENV=production # Change to 'development' for development mode

# ============================================================================
# Better Auth Configuration
# ============================================================================
# IMPORTANT: Change this secret in production!
BETTER_AUTH_SECRET=your-super-secret-key-change-in-production # Replace with a securely generated value
BETTER_AUTH_SECURE_COOKIES=false # Set to true when using HTTPS in production. Secure cookies only work in production and over HTTPS

# Base URL for Better Auth (where your app is accessible)
# Change this to your server's IP or domain
# For HTTPS production:
BETTER_AUTH_URL=https://your-domain.com
NEXT_PUBLIC_APP_URL=https://your-domain.com

# ============================================================================
# Sub-path deployment (BUILD TIME — Docker build arg, not runtime env)
# ============================================================================
# Set this when serving VyManager from a sub-path behind a reverse proxy,
# e.g. https://host/vymanager/ instead of https://host/.
# Pass as a Docker build arg: --build-arg NEXT_PUBLIC_BASE_PATH=/vymanager
# Leave unset (or empty string) for root-path deployments.
# NEXT_PUBLIC_BASE_PATH=/vymanager

# ============================================================================
# Backend API URL (RUNTIME CONFIGURABLE)
# ============================================================================
# BACKEND_URL is read at RUNTIME by the server-side API proxy routes.
# This allows you to change the backend URL without rebuilding the Docker image.
#
# For Docker Compose deployments (default): http://backend:8000
# For separate backend server: http://your-backend-ip:8000
# For local development: http://localhost:8000
BACKEND_URL=http://backend:8000

# Trusted origins for CORS (comma-separated list)
# Include all URLs where your frontend will be accessed
# Examples:
#   Single origin: http://192.168.9.105:3000
#   Multiple origins: http://192.168.9.105:3000,http://vymanager.local:3000,https://vymanager.example.com
TRUSTED_ORIGINS=http://192.168.9.105:3000,http://localhost:3000 # set IP according to your deployment


DATABASE_URL=postgresql://vymanager:vymanager_secure_password@postgres:5432/vymanager_auth
# ============================================================================
# Notes
# ============================================================================
# - Change BETTER_AUTH_SECRET to a long, random string
# - Update BETTER_AUTH_URL and NEXT_PUBLIC_APP_URL to match your deployment
# - Add all origins where users will access the app to TRUSTED_ORIGINS
# - For production, consider using HTTPS URLs
